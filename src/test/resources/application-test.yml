spring:
  datasource:
    url: jdbc:h2:mem:jiralite-test;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create-drop

jiralite:
  jwt:
    secret: test-secret-test-secret-test-secret-test-secret-test
    expiration-minutes: 60
